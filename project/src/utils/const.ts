export enum AppRoutes {
  Root = '/',
  SignIn = '/sign-in',
  Room = '/room',
  Favorites = '/favorites'
}

export enum AutorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

