export type City = {
  title: string,
  lat: number,
  lng: number,
  zoom: number,
};
export type Point = {
  title: string,
  price: number,
  type: string,
  id: number,
  lat: number,
  lng: number,
};
export type Points = Point[];
